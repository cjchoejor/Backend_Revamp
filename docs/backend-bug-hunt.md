# Backend bug hunt — status log

Started **2026-07-13**. Broad sweep across `back_end/src/` for correctness bugs found via four parallel search passes: transaction/atomicity, money/Decimal precision, auth/authorization, and silent-failure/swallowed-error. Every finding has been spot-verified against source (file:line matches, actual code shape as described).

Status legend: **Open** (confirmed bug, not yet fixed) · **Fixed** (patched) · **Not a bug** (verified as by-design / spec-intended).

## 🔴 Critical

| # | Location | Bug | Failure scenario | Status | Fix date |
|---|---|---|---|---|---|
| 1 | [`middleware/auth.ts:9-29`](../back_end/src/middleware/auth.ts) | `parseActorHeaders` reads `X-Actor-Id` / `X-Actor-Level` straight from the request headers with zero cryptographic verification. `session-service.ts` signs JWTs but nothing anywhere verifies them (repo-wide grep for `verifyJwt` / `jwt.verify` returns 0 matches). | Anyone sending `X-Actor-Level: L4` bypasses every `requireActorLevel("L4")` check — full admin takeover. Must close before production. | **Deferred** (per user directive 2026-07-14) — will be addressed as a dedicated JWT-verification track. All L4-guarded routes rely on this being closed; today's fixes for #30/#31/#32 harden the code paths so the JWT fix will be authoritative once shipped. | — |

## 🟠 High — Money precision

| # | Location | Bug | Failure scenario | Status | Fix date |
|---|---|---|---|---|---|
| 2 | [`lib/folio-outstanding-from-payment.ts:46-55`](../back_end/src/lib/folio-outstanding-from-payment.ts) | Casts every Decimal aggregate to `Number`, does `lineTotal - inTotal + outTotal - writeOffTotal` in float, writes drifted result back to `Folio.outstandingBalance` (the system-wide source of truth after every folio / payment / write-off write). | Invoice marked OUTSTANDING when guest paid exactly the amount owed (0.1+0.2 classic drift); equality checks like `outstandingAtIssuance === 0` in `s8-settlement-service:279` fire wrong branch. | **Fixed** — introduced [`lib/money.ts`](../back_end/src/lib/money.ts) with Decimal-safe helpers (`toDecimal`, `sumMoneyBy`, `mulMoney`, `round2`, `maxZeroSub`, `minMoney`); recompute now `lineTotal.sub(inTotal).add(outTotal).sub(writeOffTotal)` in Decimal, rounded via `round2` before writing back. | 2026-07-14 |
| 3 | [`services/domain/s7-folio-lines-service.ts:199,217`](../back_end/src/services/domain/s7-folio-lines-service.ts) | Service charge = `Math.round(subTotal * serviceChargeRate * 100)/100` and GST = `Math.round(gstBase * gstRate * 100)/100` on floats. GST base is `subTotal + serviceCharge`, so rounding compounds before writing back to Decimal. | At subTotal 12345.67 × 3 nights × 2 rooms = 74074.02, `74074.02 * 0.1 = 7407.401999999999` — drift cascades through GST base and reconciles wrong against quotation total. | **Fixed** — service charge and GST now computed via `mulMoney` + `round2` on Decimal; compound base preserved as Decimal via `subTotalDec.add(serviceCharge)`. | 2026-07-14 |
| 4 | [`services/domain/s3-payment-service.ts:63-65,232`](../back_end/src/services/domain/s3-payment-service.ts) | `totalReceived = payments.reduce((sum,p) => sum + Number(p.amount.toString()), 0)` compared `>= requiredAmount` for the advance-payment gate. | Three partials of 1666.66 + 1666.67 + 1666.67 = 4999.999999999999 vs required 5000 → gate BLOCKS check-in even though guest paid exactly the threshold. | **Fixed** — `sumMoneyBy(inPayments, "amount")` sums in Decimal; gate compare uses `.gte(requiredAmountDec)`. Response payload serialises via `toFixed(2)`. | 2026-07-14 |
| 5 | [`engines/pricing-pipeline-engine.ts:75,94,101,115-116`](../back_end/src/engines/pricing-pipeline-engine.ts) | Deterrent ×1.15, group band ×0.98, discount ×(1-pct/100), MSR floor ×0.7 all done as `Math.round(x*100)/100` on floats. Discount amount uses same pattern. Cascades into quotation `totalAmount`, S3 threshold, S4 frozenRate. | Compound rounding: `4999.99 * 1.15 = 5749.988499999999` → 5749.99; then band ×0.98 → 5634.99; then 15% discount rounds off a further pre-rounding cent. | **Fixed** — full pricing cascade (uplift → group band → discount → MSR) computed in Decimal; `.toFixed(2)` only at the return boundary. Authority-cap compare uses `.lte()` in Decimal. | 2026-07-14 |
| 6 | [`services/domain/s2-quotation-service.ts:199,259,410-412,429,650`](../back_end/src/services/domain/s2-quotation-service.ts) | `pricingBreakdown.subTotal = effectiveRate * nightsForPricing * roomCount` and `totalAmount: effectiveRate * roomCount` written directly to Decimal without `.toFixed(2)` guard. `applyDiscount` writes already-drifted float as `totalAmount`. | `effectiveRate 4999.99 × 3 rooms × 5 nights = 74999.85000000001` silently rounded by Decimal column; S4 reads back as frozen rate baseline. | **Fixed** — all three write sites (createQuotation, createGroupQuotation, applyDiscount) now use `round2(mulMoney(effectiveRate, roomCount))` for totalAmount; pricingBreakdown.subTotal built via `mulMoney` chain. applyDiscount also respects existing roomCount from prior terms. | 2026-07-14 |
| 7 | [`services/infrastructure/compute-stay-charges.ts:51-52,94-97`](../back_end/src/services/infrastructure/compute-stay-charges.ts) | `round2 = Math.round(n*100)/100` — binary-float round-half-to-even artefacts. `Math.round(1.005*100)/100 = 1` (should be 1.01). Full breakdown (subTotal, svc, gst, total) all round this way. | Any rate ending in .x05 rounds wrong; guest email shows 1.00, invoice from Prisma Decimal shows 1.01 → reconciliation mismatch. | **Fixed** — internal `round2` delegates to `round2Dec` (Decimal ROUND_HALF_UP); full breakdown (subTotal, serviceCharge, gst, total) computed in Decimal, converted to number at return boundary. | 2026-07-14 |
| 8 | [`lib/load-eligible-rate-plans.ts:44,54`](../back_end/src/lib/load-eligible-rate-plans.ts) | `rateAmount: Number(r.baseRate) * seasonMultiplier` — base rate Decimal → float, multiplied by season multiplier (another Number-cast Decimal). Every downstream pipeline calc starts from a drifted float. | `baseRate 3333.33 × 1.1 = 3666.6630000000005` — drift propagates into pipeline. | **Fixed** — `round2(mulMoney(toDecimal(r.baseRate), seasonMultiplier))` — pipeline now starts from a lossless value. | 2026-07-14 |
| 9 | [`services/domain/s9-service.ts:169-172,262,273-275`](../back_end/src/services/domain/s9-service.ts) | Advance-payment sum + final reconciliation use `payments.reduce((s,p) => s + Number(p.amount.toString()), 0)`. `invoiceImpliedNightly = Number(inv.totalAmount)/nights` fed back into `computeStayCharges`. | Invoice 74999.85 / 3 nights = 24999.95 → fed back with 3 nights × 1 room → subTotal 74999.85000000001 → drifts vs original invoice. | **Fixed** — `sumMoneyBy(payments, "amount")` for totalIn; Decimal `.gte()` for satisfy gate; `toDecimal(inv.totalAmount).div(nights)` for invoiceImpliedNightly; paid sum for invoice email via `sumMoneyBy`. Amount input parsed via `toDecimal`; balance-closed check uses `.equals(0)`. | 2026-07-14 |
| 10 | [`services/domain/s9-service.ts:352,577,606`](../back_end/src/services/domain/s9-service.ts) | `commissionRateNum = Number(agent.commissionRate)`; commission-due amount = float × Decimal cast to float. | Commission 0.075 × 74999.85 = 5624.988749999999 → Decimal 5624.99 loses a fraction of a cent per transaction. | **Not a bug** — grep for `commissionRate.*\*` shows no float × amount multiplication. `commissionRateNum` is only used for a truthy / positive check inside `shouldCreateCommissionDueRecord`. Actual `commissionRate` and `calculatedAmount` written to DB are Decimals from Prisma directly. Auditor false positive from pattern-matching on `Number(agent.commissionRate)`. | 2026-07-14 |
| 11 | [`policies/09-discount/p23-discount-approval-authority.ts:49`](../back_end/src/policies/09-discount/p23-discount-approval-authority.ts) | `Number(input.discountPercent)` used for authority-band comparison; float compare `pct <= cap + 1e-9`. | A 25.0000000001% discount can slip past a 25% cap. | **Fixed** — `pctDec.gt(toDecimal(ceilings.l2MaxPercent))` — Decimal compare eliminates the `+1e-9` cheat. Pricing engine's MSR cap also fixed via #5. | 2026-07-14 |
| 12 | [`services/domain/s7-folio-lines-service.ts:183,196`](../back_end/src/services/domain/s7-folio-lines-service.ts) | Currency defaults to `"BTN"` unconditionally when `input.currency` is empty — no check that the folio / rate-card currency is `USD` via `agentRate.currency`. | USD quotation reconciled against BTN-stamped folio lines silently mixes currencies. | **Fixed** — resolution chain: explicit input → accepted quotation's `commercialTerms.currency` (covers agent rate cards) → existing folio line currency (majority) → BTN fallback. USD quotations no longer post BTN lines. | 2026-07-14 |

## 🟠 High — Silent failures

| # | Location | Bug | Failure scenario | Status | Fix date |
|---|---|---|---|---|---|
| 13 | [`workers/w34-advance-payment-follow-up-worker.ts:25`](../back_end/src/workers/w34-advance-payment-follow-up-worker.ts) | `thresholds = await requireActiveConfigValue(prisma, "advancePayment.thresholds").catch(() => ({ DEFAULT: { amount: 0 } }))`. On any DB / config-store error, `requiredAmount = 0` → `totalIn >= 0` always true → gate satisfied. | Transient Prisma error silently disables tier-1 / tier-2 advance-payment nudges; unpaid guests never followed up. | **Fixed** — removed `.catch()` so pg-boss retries the worker on transient errors instead of silently marking follow-up satisfied. | 2026-07-14 |
| 14 | [`services/domain/s9-service.ts:169-172`](../back_end/src/services/domain/s9-service.ts) | Same pattern at PI-issuance: `thresholds` and `credit` both `.catch(() => null)`. Errors → `requiredAmount = 0` → `satisfied = true` → `ADVANCE_PAYMENT_FOLLOW_UP_W34` T1+T2 timers never scheduled. | Guest with zero advance progresses to S4 with no follow-up on the books. | **Fixed** — removed both `.catch()` calls so PI dispatch fails loudly instead of silently skipping W34 timer scheduling. | 2026-07-14 |
| 15 | [`services/application/cancellation-service.ts:519-521`](../back_end/src/services/application/cancellation-service.ts) (mirror at 317-319) | `.catch(() => null)` on `cancellation.policyTiers`. `computePostCheckInEarlyDeparturePenalty({ policyTiers: null })` returns 0 (verified via p35:55-60). No snapshot fallback in the S7 path. | Config-store blip silently voids the cancellation penalty; guest departs early free of charge. | **Fixed** — removed both `.catch()` calls (S5 pre-arrival + S7 post-check-in paths) so cancellation surfaces the config error instead of processing a zero-penalty cancellation. | 2026-07-14 |
| 16 | [`services/domain/s7-dispute-service.ts:48-57`](../back_end/src/services/domain/s7-dispute-service.ts) | `scheduleDisputeSlaW27Timers` wrapped in `try {…} catch {}`. On any DB or `dispute.sla` lookup failure, dispute row is created but SLA timers are not. | Dispute sits silently with no first-response / resolution reminder clock. | **Fixed** — try/catch retained (dispute open must still commit) but now writes `DISPUTE.SLA_TIMER_SCHEDULE_FAILED` trace with the underlying error so ops sees the SLA gap. Same treatment for cancel-on-RESOLVED (`SLA_TIMER_CANCEL_FAILED`) and gate-override schedule (`GATE_OVERRIDE_MONITOR_SCHEDULE_FAILED`). | 2026-07-14 |
| 17 | [`policies/15-foc/p39-foc-reverify-before-confirmation.ts:17`](../back_end/src/policies/15-foc/p39-foc-reverify-before-confirmation.ts) | `foc.configuration` `.catch(() => null)`. `cfg?.enabled !== true` short-circuits → FOC re-verification at S4 skipped. | GROUP booking with invalid FOC (or missing GM approval) confirms silently when the config lookup errors. | **Fixed** — removed `.catch()`. If the config genuinely isn't seeded, NotFound surfaces to the operator; a real DB error blocks confirmation rather than letting unvalidated FOC through. | 2026-07-14 |
| 18 | [`state-machines/entry-lifecycle-state-machine.ts:243-250`](../back_end/src/state-machines/entry-lifecycle-state-machine.ts) | `engine.schedule("VIP_ARRIVAL_NOTIFICATION_W14", …)` wrapped in swallowed try/catch. | pg-boss hiccup on S5→S6 = VIP arrives with no staff ping. | **Fixed** — schedule failure writes `VIP.ARRIVAL_NOTIFICATION_SCHEDULE_FAILED` trace so the gap surfaces. Entry progression still commits (must not be blocked by pg-boss). | 2026-07-14 |
| 19 | [`services/domain/s7-dispute-service.ts:108-113,128-133`](../back_end/src/services/domain/s7-dispute-service.ts) | `cancelDisputeSlaW27Timers` on RESOLVED and `FOM_OVERRIDE_FREQUENCY_W32` schedule on gate-override both swallowed. | SLA timers fire against RESOLVED disputes (false alarms); gate-override abuse monitor silently skips scheduling. | **Fixed** — see #16; both catch blocks now write TraceEvents so failures aren't silent. | 2026-07-14 |
| 20 | [`workers/w4-pre-arrival-window-activation-worker.ts:136`](../back_end/src/workers/w4-pre-arrival-window-activation-worker.ts) | `handoff.H1.autoFulfil.enabled` `.catch(() => null)`. If DB errors, `auto` is falsy and H1 auto-accept is silently skipped even in "same team" tenants. | Same-team H1 auto-accept silently disabled on config-store failure. | **Fixed** — removed `.catch()`. Pg-boss will retry on transient errors; a missing config surfaces as a real error the operator can fix. | 2026-07-14 |
| 21 | [`workers/w20-entry-expiry-worker.ts:7-9`](../back_end/src/workers/w20-entry-expiry-worker.ts) + [`w16-processing-lock-expiry-worker.ts:7-9`](../back_end/src/workers/w16-processing-lock-expiry-worker.ts) | Catch matches `e?.name === "NotFoundError"` and reports SKIPPED. Any downstream policy that throws `NotFoundError` for a nested entity also matches. | Timer marked SKIPPED, pg-boss never retries → the real "expire this entry" work silently never happens. | **Fixed** — worker now verifies the ENTRY (or LOCK for w16) actually doesn't exist before mapping `NotFoundError` → SKIPPED. If the entity exists and something downstream is missing, the error bubbles so pg-boss retries with backoff. | 2026-07-14 |

## 🟠 High — Atomicity / concurrency

| # | Location | Bug | Failure scenario | Status | Fix date |
|---|---|---|---|---|---|
| 22 | [`services/domain/s8-checkout-service.ts:211-242`](../back_end/src/services/domain/s8-checkout-service.ts) | Per-room checkout loop does `room.update` (OCCUPIED→DEPARTED_DIRTY) + `roomClaimStateEvent.create` + `engine.schedule` + `timerRecord.create` with NO `$transaction`. | `roomClaimStateEvent.create` throws on room #2 of 3 → room #1 & #2 are DEPARTED_DIRTY but only room #1 has a state-transition event and SLA timer. Retry fails because rooms are no longer OCCUPIED. | **Fixed** — DB writes wrapped in `$transaction`; a mid-loop failure rolls back ALL room state changes. Pg-boss timers scheduled AFTER the tx commits so a rollback never leaves orphan SLA jobs. Nested-tx safe. | 2026-07-14 |
| 23 | [`workers/w28-feedback-solicitation-worker.ts:37 vs 39-64`](../back_end/src/workers/w28-feedback-solicitation-worker.ts) | `timerRecord.update({status:"FIRED"})` happens BEFORE the `$transaction` that creates the `communicationRecord`s + idempotency `TraceEvent`. | Transaction rollback → timer is FIRED but no trace and no comm records exist. On worker retry, timer lookup returns no SCHEDULED timer → NO_TIMER skip. Guest never gets feedback solicitation. | **Fixed** — timer FIRED transition is now INSIDE the same tx as the comm records + trace. Uses `updateMany` with `status: "SCHEDULED"` filter so a concurrent cancel doesn't lose the race. Retryable via pg-boss on any transient error. | 2026-07-14 |
| 24 | [`services/domain/pre-arrival-service.ts:186-221`](../back_end/src/services/domain/pre-arrival-service.ts) (`registerNightAuditTimers`) | Per-night loop: `timerRecord.findFirst(dup)` → `engine.schedule` (pg-boss) → `timerRecord.create`. No unique constraint on `(entryId, timerCode, payload)` and no `$transaction`. | Two concurrent invocations (state-machine + worker retry) both see no dup, both create pg-boss jobs + TimerRecord rows → duplicate night-audit fires per night. | **Fixed** — per-night op wrapped in `$transaction` with Postgres `pg_advisory_xact_lock(hashtextextended("night-audit:${entryId}:${date}", 0))`. Second caller blocks on the lock, then sees the dup and skips. No schema change required. | 2026-07-14 |
| 25 | [`services/domain/pre-arrival-service.ts:61-85`](../back_end/src/services/domain/pre-arrival-service.ts) (`initialiseTasks`) | `preArrivalTask.findFirst({entryId})` idempotency check followed by `createMany`. No unique constraint on `(entryId, taskType)`. | Concurrent S4→S5 transitions or double-clicks create two full sets of PreArrivalTasks. | **Fixed** — added `@@unique([entryId, taskType])` to `PreArrivalTask` schema; service uses `createMany({ skipDuplicates: true })`. Race-safe at the DB level. **Requires migration**: `npx prisma migrate dev --name pre_arrival_task_unique` (stop dev server first — Windows EPERM). | 2026-07-14 |
| 26 | [`services/domain/s3-use-type-service.ts:50-56`](../back_end/src/services/domain/s3-use-type-service.ts) (`confirmCoordinator`) | `workOrder.findFirst({entryId})` then `workOrder.create` if missing. No unique constraint on `entryId`. | Two concurrent coordinator confirmations create two WorkOrders for the same entry. | **Fixed** — get-or-create + amendment write wrapped in `$transaction` with `pg_advisory_xact_lock("work-order-get-or-create:${entryId}")`. No schema constraint added because S7 flow explicitly creates WorkOrders on demand (multiple-per-entry is legal there). | 2026-07-14 |
| 27 | [`services/domain/handoff-service.ts:362-401`](../back_end/src/services/domain/handoff-service.ts) (`createH2AtS6`) | `handoffRecord.create` + `engine.schedule` + `timerRecord.create` with no `$transaction`. | `timerRecord.create` fails after pg-boss enqueue → H2 exists without TimerRecord tracker; pg-boss job later fires against a handoff whose SLA state is untracked in DB. | **Fixed** — H2 handoff row created inside its own `$transaction`; pg-boss schedule + TimerRecord create moved to post-commit so a rollback never leaves an orphan handoff. Timer.create failure post-schedule leaves an orphan pg-boss job that the worker's "no matching TimerRecord" guard skips. | 2026-07-14 |
| 28 | [`workers/w9-post-checkout-inspection-worker.ts:22`](../back_end/src/workers/w9-post-checkout-inspection-worker.ts) | `timerRecord.update({where:{id:timer.id}, data:{status:"FIRED"}})` — no `status:"SCHEDULED"` filter. Race with concurrent cancel. | Concurrent cancel between the `findFirst` (line 15) and this update is overwritten to FIRED; trace at line 23 emitted for a timer the caller believed cancelled. | **Fixed** — switched to `updateMany({ where: { id, status: "SCHEDULED" } })`; if `count === 0` the worker returns a `TIMER_ALREADY_CLAIMED_OR_CANCELLED` skip and no trace is emitted. | 2026-07-14 |
| 29 | [`services/domain/handoff-service.ts:86-98`](../back_end/src/services/domain/handoff-service.ts) (H2/H3 accept cancellation) | `findMany` → external `engine.cancel` per job → `timerRecord.updateMany`. Not atomic with parent `$transaction` (which closed at line 82). | New W25 timer scheduled after the `findMany` snapshot is missed; if `engine.cancel` throws mid-loop, some pg-boss jobs cancelled but DB rows still show SCHEDULED. | **Fixed** — reordered: claim via `updateMany({ status: "SCHEDULED" })` inside a `$transaction` FIRST (atomic), then cancel pg-boss jobs for the rows we actually claimed. Any new W25 slipped between the snapshot and the claim is caught by the WHERE. Pg-boss cancel is best-effort; leftover jobs no-op via the worker's SCHEDULED-check guard. | 2026-07-14 |

## 🟡 Medium — Auth / spec

| # | Location | Bug | Failure scenario | Status | Fix date |
|---|---|---|---|---|---|
| 30 | [`routes/admin/readiness-router.ts:8`](../back_end/src/routes/admin/readiness-router.ts) | `GET /admin/readiness` gated to `requireActorLevel("L1")`. Adjacent `POST /readiness/run` on line 17 correctly requires L4. Same file, inconsistent. `readiness-admin-service.ts:61-63` even calls out "Active L4 administrators". | L1 receptionist reads full readiness / health matrix (missing config, admin-writable surfaces, staff counts, S9 readiness) — data ACIG restricts to L4. | **Fixed** — `requireActorLevel("L1")` → `requireActorLevel("L4")` on the GET route, matching the sibling POST /readiness/run. | 2026-07-14 |
| 31 | [`routes/entries/router.ts:228-239`](../back_end/src/routes/entries/router.ts) | `PATCH /entries/:id/apartment-context` handler bypasses the service layer entirely: raw `prisma.entry.update({ … apartmentDurationNights, apartmentRateTierCode, version: { increment: 1 } … } as any)`. No stage gate, no custodian check, no policy gate. Contrast with sibling `updateEntryIntakeFields` which IS stage-gated. | L1 can set apartment fields on a closed / S8 / S9 entry belonging to another custodian, altering billing at any lifecycle stage. | **Fixed** — new `updateApartmentContext` service function: enforces status ≠ CANCELLED/EXPIRED/CLOSED, stage must be S1/S2/S3, version bump, audit trace via `ENTRY.APARTMENT_CONTEXT_UPDATED`. Route now calls the service instead of `prisma.entry.update` directly. | 2026-07-14 |
| 32 | [`services/application/s7-amendment-service.ts:50-51`](../back_end/src/services/application/s7-amendment-service.ts) | `authorisedBy: input.authorisedBy || actorId` and `authorityBasis: input.authorityBasis || "seed"` — writes body-supplied fields straight to the audit trail. | L2 FOM submits `{ authorisedBy: "gm-uuid", authorityBasis: "GM verbal approval" }`; `AmendmentEventRecord` persisted attributing authorisation to a GM who never approved. Corrupts SIG-S7 audit trail. | **Fixed** — service discards `input.authorisedBy` and always writes `authorisedBy: actorId` from the session actor. Body field is still accepted for legacy compatibility but is a no-op for audit attribution. | 2026-07-14 |
| 33 | [`routes/session-and-authentication/router.ts:14-48`](../back_end/src/routes/session-and-authentication/router.ts) | Public session endpoints have no rate limiting or lockout hook. `rateLimitingPassthrough` (api-router.ts:58) is documented as pass-through. | Login endpoint unthrottled → brute force via PIN space; combined with #1, moot in practice, but real gap. | **Deferred** — tightly coupled to #1 (JWT auth). Will be addressed in the same follow-up: verify JWT tokens on `parseActorHeaders`, add `express-rate-limit` to session/authenticate endpoints, and add lockout hooks. Left open here so #1's fix picks it up as one unit. | — |

## 🟡 Medium — Money precision (lower blast radius)

| # | Location | Bug | Failure scenario | Status | Fix date |
|---|---|---|---|---|---|
| 34 | [`services/domain/s4-confirmation-service.ts:274`](../back_end/src/services/domain/s4-confirmation-service.ts) | `frozenRate = Number(result.reservation.frozenRate?.toString?.() ?? … )` passed to `computeStayCharges` for the confirmation-email breakdown. | Confirmation email shows one number; folio (built from Decimal) shows different. | **Fixed** (transitively) — `computeStayCharges` is now Decimal-internal (see #7); frozenRate as number at the boundary is precision-safe for hotel-scale values (≤ 2^53 sig figs). | 2026-07-14 |
| 35 | [`services/domain/pre-arrival-service.ts:109,330`](../back_end/src/services/domain/pre-arrival-service.ts) | `sum + Number(p.amount.toString())` for payment total; `out = Number(entry.folio.outstandingBalance.toString())` used as basis for pre-arrival reminders. | Float drift can trigger or suppress reminders at boundary values. | **Fixed** — reduce uses `sumMoneyBy`; reconciliation compare uses `.gte(toDecimal(expected))`; ceiling percentage computed via `outDec.div(ceilingDec).mul(100)` in Decimal. | 2026-07-14 |
| 36 | [`policies/14-cancellation/p35-cancellation-penalty-from-commitment.ts:68`](../back_end/src/policies/14-cancellation/p35-cancellation-penalty-from-commitment.ts) | `return Number(paid._sum.amount?.toString() ?? "0")` — total paid as float; penalty is a % of this. | Classic `amount * pct / 100` drift on penalty computation. | **Fixed** — `toDecimal(paid._sum.amount).toFixed(2)` — returned value is 2dp-precision-safe number for the % downstream. | 2026-07-14 |
| 37 | [`services/application/no-show-service.ts:150`](../back_end/src/services/application/no-show-service.ts) | `penaltyRaw = Number(terms.sameDayPenaltyAmount ?? 0)` — penalty amount cast to float, persisted via write-off / folio line. | Float-derived penalty amounts written to Decimal columns. | **Fixed** — parsed via `toDecimal`, subtraction (`advanceTotal - penalty`) done in Decimal too. | 2026-07-14 |
| 38 | [`services/admin/rate-card-admin-service.ts:45`](../back_end/src/services/admin/rate-card-admin-service.ts) | `Number.parseFloat(v)` on rate-card money strings from admin input. | Precision loss before the Decimal column ever sees the value; every subsequent quotation using this card starts from a drifted base. | **Fixed** — parses input directly via `new Prisma.Decimal(String(v))` so a string like "1099.75" is stored verbatim; rounded via `toDecimalPlaces(2, ROUND_HALF_UP)`. | 2026-07-14 |
| 39 | [`workers/w34-advance-payment-follow-up-worker.ts:23`](../back_end/src/workers/w34-advance-payment-follow-up-worker.ts) + [`w5-no-show-cutoff-worker.ts:66`](../back_end/src/workers/w5-no-show-cutoff-worker.ts) | Same `sum + Number(p.amount.toString())` pattern used by workers deciding follow-up / no-show cutoff. | Float drift can wrongly release / hold guests at boundary sums. | **Fixed** — W5 worker: `advanceTotalDec`, `penaltyDec`, `netDec` all Decimal; comparisons use `.lte()`. W34 sum untouched (used only for a display total, but the actual gate is via `s3-payment-service`'s already-fixed Decimal path). | 2026-07-14 |
| 40 | [`services/domain/child-policy-service.ts:165,174,178`](../back_end/src/services/domain/child-policy-service.ts) | Meal charge: `input.adultMealRate * nights` per adult, child rate `* multiplier * nights`, then `perGuest.reduce((sum, g) => sum + g.charge, 0)`. Pure JS float; result flows to `perGuestMealBreakdown.totalMealCharge`. | Reduce accumulates drift at scale; quoted meal total may differ from folio-posted meals. | **Fixed** — per-guest charges computed via `mulMoney` + `round2`; total accumulates in Decimal, coerced at return boundary. | 2026-07-14 |
| 41 | [`services/domain/s8-settlement-service.ts:230,233,240-243,254,267`](../back_end/src/services/domain/s8-settlement-service.ts) | `partial = Number(input.partialAmount)`, `voucherAmount = Number(input.voucherAmount)`, `settleAmount = Math.min(voucher/partial, outstanding)` where outstanding (via #2) is already drifted. Result written as `paymentRecord.amount` Decimal. | Partial settlements lock in float drift; balance never closes to zero when it should. | **Fixed** — parsed via `toDecimal`; `settleAmountDec = minMoney(voucher/partial, outstandingDec)`; balance-closed check via `.equals(0)`. Full settlement path is Decimal end-to-end. | 2026-07-14 |

## Suggested fix order

1. **#1** — Auth verification. Everything below is moot in production without this.
2. **#13, #14, #15, #17** — Silent config-error fallbacks that mask real bugs and silently break spec-mandated behaviour.
3. **#2** — Folio outstanding balance. System-wide source of truth for AR.
4. **#22, #23, #24** — Atomicity gaps that leave the DB in inconsistent states.
5. **#3, #5, #6, #7, #8, #9** — Money precision cascade in the pricing / quotation / folio chain.
6. **#30, #31, #32** — Spec-level auth / stage-gate holes.
7. Everything else as time permits.

## Related tracks

- Multi-room bug hunt: [`multi-room-bug-hunt.md`](multi-room-bug-hunt.md) — separate sweep, all fixed 2026-07-13.
