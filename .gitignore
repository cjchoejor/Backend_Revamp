# Local / reference material — excluded from version control (see repo policy)
DEV-SPEC finalization/
back_end/Test_ReVamp/
Documentation/
Documentation_V2/
BACKEND-STRUCTURAL-ATLAS-v1_1 (1).html
LEGPHEL_Implementation_Reference_v1_1.html

# Generated PDF artifacts (guest-facing bills / vouchers). One-shot write-once files —
# they should live on durable storage (S3 / GCS) in prod, and locally in dev.
back_end/storage/
