/**
 * Atlas Cat 13 §10.2.17 / Cat 10 route group — Communications.
 * Outbound quotation comms and timers are created inside `s2-quotation-service.sendQuotation`; there is no standalone communications router yet.
 */
export type CommunicationsAtlasPlaceholder = Record<string, never>;
